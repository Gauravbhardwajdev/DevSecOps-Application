apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-app
  namespace: prod
spec:
  replicas: 2
  selector:
    matchLabels:
      app: nodejs-app
  template:
    metadata:
      labels:
        app: nodejs-app
    spec:
      # THIS SECTION IS REQUIRED
      imagePullSecrets:
        - name: regcred 
      containers:
        - name: nodejs-app
          image: gauravbhardwajb96/nodejs-app:prod-504c4b08c84b09504f64a0950a1ea5b4302990eb
          ports:
            - containerPort: 5000
          # ... (rest of your configuration)
          env:
            - name: DB_HOST
              value: mysql  # Ensure this points to your production service
            - name: DB_NAME
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_DATABASE            
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: mysql-secret
                  key: MYSQL_PASSWORD
            - name: DATABASE_URL
              value: "mysql://$(DB_USER):$(DB_PASSWORD)@$(DB_HOST):3306/$(DB_NAME)"
