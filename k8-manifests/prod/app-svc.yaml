apiVersion: v1
kind: Service
metadata:
  name: nodejs-service
  namespace: prod
spec:
  selector:
    app: nodejs-app
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort
